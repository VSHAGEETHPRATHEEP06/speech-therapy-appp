// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';
//
// // Make sure to import your app's main entry point
// import 'package:speech_therapy_app/main.dart'; // Replace with your actual import
//
// void main() {
//   testWidgets('Counter increments smoke test', (WidgetTester tester) async {
//     // Build your app and trigger a frame
//     await tester.pumpWidget(MyApp()); // Replace with your app's actual entry point
//
//     // Verify that the counter starts at 0
//     expect(find.text('0'), findsOneWidget);  // Replace with the widget containing the initial value
//     expect(find.text('1'), findsNothing);
//
//     // Tap the '+' icon and trigger a frame
//     await tester.tap(find.byIcon(Icons.add));  // Ensure your app has this icon
//     await tester.pump();
//
//     // Verify that the counter has incremented
//     expect(find.text('0'), findsNothing);
//     expect(find.text('1'), findsOneWidget);
//   });
// }
